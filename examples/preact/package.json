{
  "private": true,
  "name": "preact-example",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm typecheck && preact build --no-sw",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch --no-sw",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0"
  },
  "dependencies": {
    "@fingerprint/react": "workspace:*",
    "preact": "10.29.7",
    "preact-render-to-string": "^6.7.0"
  },
  "devDependencies": {
    "preact-cli": "^3.5.1",
    "sirv-cli": "^3.0.1",
    "typescript": "catalog:"
  }
}
